{% extends 'base.html.twig' %}

{% block title %}Lists index{% endblock %}

{% block body %}
    <h1>Lists index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for list in lists %}
            <tr>
                <td>{{ list.id }}</td>
                <td>{{ list.name }}</td>
                <td>
                    <a href="{{ path('app_lists_show', {'id': list.id}) }}">show</a>
                    <a href="{{ path('app_lists_edit', {'id': list.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lists_new') }}">Create new</a>
{% endblock %}
